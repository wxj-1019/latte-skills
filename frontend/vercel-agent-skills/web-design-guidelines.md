# Web Design Guidelines

> ⚠️ **本文件已去重。** 权威版本见独立的 **`web-design-guidelines`** 技能（现位于顶层 `~/.zcode/skills/web-design-guidelines/`）。它会在运行时拉取 Vercel 最新的 Web Interface Guidelines，并按 `file:line` 格式输出问题位置，便于直接修改。

本 bundle（vercel-agent-skills）原先在此维护的精简中文规则清单已停用，以避免与独立原版产生两份漂移、各自过期。

需要进行 Web 规范 / 可访问性 / 性能 / UX 合规审查时，请直接使用顶层技能 **`web-design-guidelines`**。
